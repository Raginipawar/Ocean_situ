# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cesium's static assets (Workers/Assets/Widgets/ThirdParty), copied from
# node_modules by the `postinstall` script in package.json -- regenerated
# automatically on `npm install`, not checked in (7+MB, derived, not source).
public/cesium
