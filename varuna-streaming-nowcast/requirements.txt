# Exact versions verified working on Windows 11 (CPU-only, no GPU/CUDA needed --
# this track deliberately never installs mamba-ssm or any compiled-kernel
# dependency; every tensor op here runs identically on CPU across
# Windows/Mac/Linux).
# Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

numpy==1.26.4
torch==2.5.1
matplotlib==3.10.6

pydantic==2.10.3

pytest==7.4.4

# Optional, only needed for the Day 3 real-feed HTTP adapter:
# httpx==0.28.1
